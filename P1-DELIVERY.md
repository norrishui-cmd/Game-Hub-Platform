# GameRadar P1 完整覆盖包

本包是在已通过回归的 P0 基线上继续完成的 P1，可直接覆盖原项目目录。请保留部署平台中的环境变量，不要把密钥写进仓库。

## 本次交付

- 新增五语言 `/games/` 游戏浏览中心：关键词、发行状态、平台、类型、内容深度、排序、URL 查询参数和渐进加载。
- 升级全站搜索：标题/开发商/类型/平台联想、轻量模糊匹配、上下键选择、Enter 跳转、Esc 关闭。
- 新增发行计划器：今天、未来 7 天、本月、全部已定档、TBA、平台筛选、浏览器关注列表和 `.ics` 日历导出。
- 详情页新增平台与发行矩阵、发行关注按钮、最新外部动态、来源数量和结构化数据更新时间。
- 新增移动端汉堡导航和五语言下拉切换，补齐搜索框与交互控件的 ARIA 状态。
- 收紧新闻收录规则：纯外链信息流保持 `noindex,follow`；只有至少两条同时具备原创摘要及玩家影响分析的英文游戏新闻页才可进入 sitemap。
- sitemap 中新增 5 个语言版本的 `/games/`，并为其写入数据更新时间。

## 覆盖部署

1. 备份线上项目和部署平台环境变量。
2. 用本包中的 `Game-Hub-Platform-main/` 整体覆盖原项目目录。
3. 在项目目录执行：

   ```bash
   npm ci
   npm run build
   npm run seo:audit
   ```

4. 部署 `dist/`，或按现有 Vercel 配置直接部署仓库。
5. 上线后抽查 `/en/games/`、`/zh/games/`、`/en/releases/`、任一游戏详情页、`/robots.txt` 与 `/sitemap-index.xml`。

## 验证结果

- Astro 生产构建：通过，2,192 个 HTML 页面。
- SEO 审计：通过，2,192 个 HTML 页面，包含 canonical、noindex/sitemap 一致性、结构化数据、站内链接、广告配置和封面文件检查。
- 数据抓取链路测试：通过，1/1。
- sitemap：131 个可收录业务 URL；其中 48 个质量达标游戏 Hub、5 个游戏浏览中心。当前外部新闻聚合页和游戏新闻页均不进入 sitemap。

## 数据更新提示

代码与索引逻辑已通过，但当前快照中的 `games.json`、`monthly-trending.json`、`news.json` 超过日常更新时限。上线环境配置现有数据源凭据后，请运行相应抓取任务并重新构建；数据健康脚本会继续给出提醒，但不会阻塞本包构建。

